log_level = "debug"

[feishu]
app_id = "cli_xxxxxxx"
app_secret = "xxxxxxxxx"

[kiro]
cmd = "kiro-cli"
args = ["acp", "--agent", "lark"]
# 进程池大小，通过 thread_id hash 路由实现并行处理，默认 4
# pool_size = 4

[storage]
# 文件和视频的保存目录，默认 ~/.acp-link/data
# save_dir = "/path/to/your/data"
